# Ruby Quiz

## Requirements
* Ruby 2.5.0

## Development

Once cloned run the following command from the repo directory to run the setup

* `bin/setup`

If that doesn't work

* `bundle install`
* `bin/rails db:setup`
